const generateMenuFilters = () => {
  return (
    [`Everything`, `Future`, `Past`]
  );
};

const generateMenuControls = () => {
  return (
    [`Table`, `Stats`]
  );
};

export {generateMenuFilters, generateMenuControls};

